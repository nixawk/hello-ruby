str1 = String.new("python")
str2 = String.new("ruby")

# These instances are both derived from the same class, but they have unique
# characteristics. 
# - First, every object has a unique bject identifier (abbreviated as object as).
# - Second, you can define instance variables, variables with values that are unique to each instance.
# These instance variabes hold an object's state. 